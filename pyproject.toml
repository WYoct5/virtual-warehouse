[project]
name = "virtual-warehouse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "WYoct5", email = "tetu.8765.undergrund@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
virtual-warehouse = "virtual_warehouse:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
